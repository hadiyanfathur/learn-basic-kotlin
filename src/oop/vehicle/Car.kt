package oop.vehicle

/**
 * Data Class
 * 1. Data Class generate toString(), equals(), hashCode(), and copy()
 * 2. Data Class must have at least 1 parameter
 */
data class Car(val name: String, val brand: String){

}
